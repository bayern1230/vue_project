 # 打开方式
App running at:
  - Local:   http://localhost:8081/
  - Network: http://192.168.0.108:8081/

# 文件框架
main.js -- 入口文件
APP.vue -- 父组件，引入所有组件
# 笔记
P63 render 中提到了引入vue文件到底是哪里的文件，引入的文件并非完整版，原因：不能在webpack打包时将模板打包进去,
    只需要将vue核心打包即可
P64 修改默认配置：修改入口文件、关闭语法检查
P65 在标签中加入ref="",利用this.$refs得到真实DOM元素或者VueComponen 对象
P66 props:组件复用,props比data优先级更高
P67 mixin:混合，局部混合、全局混合
P68 插件：本质为对象，可以增强
P69 scoped:局部使用
P70 实际项目：ToDoList
P75 list.reduce()用法
P79 用本地存储方法存储数据，涉及到：深度监视、JSON转换
P80 自定义事件 .native：使用原生事件，如click.native
P84 全局事件组件:任意组件通信

路由：key-value对应关系 实现SPA single page web application
      key:路径 value:函数/组件 P117-18:48